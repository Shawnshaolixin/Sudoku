org.gradle.jvmargs=-Xmx**JVM_HEAP_SIZE**M
org.gradle.parallel=true
unityStreamingAssets=**STREAMING_ASSETS**
# Android Resolver Properties Start
android.useAndroidX=true
android.enableJetifier=true
# Android Resolver Properties End
# AGP 8.4 官方测试到 compileSdk 34，我们用到 35（Firebase BoM 34.17 要求），压制警告
android.suppressUnsupportedCompileSdk=35
# AGP 8.4 要求 JDK 17，但 Unity 2022.3 的 JDK 校验只接受 11：
# 这里让 Gradle 守护进程（AGP 运行处）改用 JDK 17，Unity 侧保持 JDK 11 不动
org.gradle.java.home=C\:\\Program Files\\Eclipse Adoptium\\jdk-17.0.20.8-hotspot
**ADDITIONAL_PROPERTIES**
